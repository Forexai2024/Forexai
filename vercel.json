{
  "version": 2,
  "builds": [
    { "src": "index.js", "use": "@vercel/node" }  // Example configuration, adjust as needed
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.js" }
  ]
}
